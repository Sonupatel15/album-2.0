streamlit==1.22.0
deepface==0.0.79
opencv-python-headless==4.7.0.72  # Must be headless for cloud deployments
numpy==1.24.0
Pillow==9.5.0
tensorflow==2.10.0  # Pinned (2.10.0 is stable with DeepFace)
pandas==1.5.0  # Pinned to avoid conflicts
tqdm==4.64.0  # Pinned
gdown==4.5.1  # Pinned
